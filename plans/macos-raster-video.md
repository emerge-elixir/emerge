# macOS raster video

- Add bounded, validated RGBA frame submission to the macOS host protocol and reuse CPU video rendering.
- Enable the demo's native headless producer without an environment gate.
- Isolate unsupported/failing streams and show their status in the Video Interop tab.
- Test frame validation, independent failures, native rendering, and live macOS playback.
